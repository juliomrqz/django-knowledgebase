============
Installation
============

At the command line::

    $ easy_install django-knowledgebase

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-knowledgebase
    $ pip install django-knowledgebase
